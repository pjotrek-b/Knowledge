File-manager Thunar's bookmarks (favorites/shortcuts) to storage locations are stored in:

`~/.config/gtk-3.0/bookmarks`

:)


NOTE: Close /all/ thunar instances before editing those files. Otherwise you may render your edits useless, and have them overwritten by a running thunar on exit ;)

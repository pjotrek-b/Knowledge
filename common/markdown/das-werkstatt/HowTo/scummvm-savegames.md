# Savegames in ScummVM (Linux)

## Location

If the `savepath` is not set in the configuration, the Default location on Xubuntu (and possibly other Debian-based distros) is:

`$HOME/.local/share/scummvm/saves/`
